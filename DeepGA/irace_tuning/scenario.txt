# ==============================================================================
# Configuración del Escenario de irace para DeepGA
# ==============================================================================

# Archivo con la definición de parámetros
parameterFile = "parameters.txt"

# Directorio de ejecución
execDir = "./"

# Script ejecutable target-runner (usar "./target-runner" en Linux/macOS o "./target-runner.bat" en Windows)
targetRunner = "./target-runner"

# 1. Instancias de Entrenamiento (5 datasets para el proceso de tuning/racing)
trainInstancesFile = "train_instances.txt"

# 2. Instancias de Validación / Test (2 datasets no vistos para evaluar la generalización)
testInstancesFile = "test_instances.txt"

# Número de configuraciones élite a evaluar sobre las instancias de prueba (1 = mejor candidato)
testNbElites = 1

# Evaluar únicamente los élites finales al concluir la búsqueda (0)
testIterationElites = 0

# Presupuesto total de experimentos (evaluaciones de candidatos de DeepGA en entrenamiento)
# Para ~20 parámetros, irace requiere al menos 504 con firstTest=5, o >=280 con firstTest=3
maxExperiments = 600

# Número de instancias evaluadas antes del primer descarte estadístico (3 a 5 para 5 instancias de train)
firstTest = 3

# Aleatorizar y balancear el orden de presentación de las instancias de entrenamiento
sampleInstances = 1

# Paralelismo: número de evaluaciones concurrentes
# Incrementar si la máquina cuenta con múltiples GPUs o núcleos CPU abundantes
parallel = 1

# Test estadístico de descarte ('F-test' recomendado para problemas continuos y ML)
testType = "F-test"

# Semilla para reproducibilidad del tuning
seed = 42

# Guardado del log de progreso y modelos probabilísticos de irace
logFile = "irace.Rdata"

# Nivel de detalle
debugLevel = 0

# Modo elitista activo
elitist = 1
